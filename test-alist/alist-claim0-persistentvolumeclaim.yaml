apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: alist-claim0
  name: alist-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
