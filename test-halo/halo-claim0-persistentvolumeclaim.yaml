apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: halo-claim0
  name: halo-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
