apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: halodb-claim0
  name: halodb-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
